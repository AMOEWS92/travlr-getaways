<h1>{{trip.name}}</h1>

<img src="/images/{{trip.image}}" alt="{{trip.name}}" style="max-width:400px; height:auto;"/>

<p><strong>Resort:</strong> {{trip.resort}}</p>
<p><strong>Length:</strong> {{trip.length}} days</p>
<p><strong>Start Date:</strong> {{trip.start}}</p>
<p><strong>Price per person:</strong> ${{trip.perPerson}}</p>

<p>{{trip.description}}</p>

<p><a href="/travel">← Back to all trips</a></p>
